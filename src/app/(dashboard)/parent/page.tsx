import { type ReactElement } from "react";

export interface pageProps {}

export default function Parentpage({}: pageProps): ReactElement {
  return (
    <>
      <div>ParentPage</div>
    </>
  );
}
